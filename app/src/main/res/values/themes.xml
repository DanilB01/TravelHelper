<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TravelHelper" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TravelHelper" parent="Base.Theme.TravelHelper" />

    <style name="Theme.TravelHelper.Init" parent="Theme.TravelHelper"/>

    <style name="Theme.TravelHelper.Init.RadioButton" parent="Theme.TravelHelper">
        <item name="colorControlNormal">@color/init_button_bg</item>
        <item name="colorControlActivated">@color/init_button_bg</item>
    </style>
</resources>