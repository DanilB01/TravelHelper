<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TravelHelper" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.TravelHelper" parent="Base.Theme.TravelHelper" />

    <style name="Theme.TravelHelper.Init" parent="Theme.TravelHelper"/>

    <style name="Theme.TravelHelper.Init.RadioButton" parent="Theme.TravelHelper.Init">
        <item name="colorPrimary">@color/init_button_bg</item>
    </style>

    <style name="Theme.TravelHelper.Splash" parent="Base.Theme.TravelHelper">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>
</resources>